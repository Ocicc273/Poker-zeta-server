/**
 * Poker Zeta — Punto di ingresso dei test
 *
 * Un file solo che importa gli altri: niente ricerca automatica
 * dei file, niente pattern da far combaciare. Si esegue con
 * `npm test`, e gira anche durante il build su Railway.
 *
 * Ogni nuovo file di test va aggiunto qui sotto, altrimenti non
 * viene eseguito.
 */

import './bot.test.js';
import './table-config.test.js';
import './twister.test.js';
